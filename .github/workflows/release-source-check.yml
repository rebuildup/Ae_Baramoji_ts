# Validates that PRs targeting `main` come from a `release-<major>-<minor>-<patch>`
# branch (policy §11 / ADR-0013), or from the auto-generated
# `tmp/release-sync/v<tag>` parent-pointer branch that release.yml uses to
# bump the submodule pointer after a tag push. For non-`main` PRs (ticket
# branches stacked on a release branch), the check exits successfully
# without comment — stacked PR inspection belongs to the integration gate,
# not the source check.
#
# Status check name emitted: `release-source` (matches the ADR-0013
# ruleset required context).
# Required check semantics: see `Protect main` ruleset + ADR-0013.

name: release-source

on:
  pull_request:
    # `edited` is required so the check re-evaluates when the PR is retargeted
    # to/from `main` (e.g. a stacked release-branch PR later flips its base
    # to main). Without it, the gate would stay stale against a base change.
    # Title / body edits do trigger extra runs, but the job body short-
    # circuits via the base-change predicate below so non-base edits pay only
    # the runner-startup cost.
    types: [opened, reopened, synchronize, edited]

permissions:
  pull-requests: read

# Cancel in-flight runs for the same PR — saves runner minutes on rapid
# rebases and avoids a stale-status window.
concurrency:
  group: release-source-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  release-source:
    name: release-source
    # Single-job workflow so the status-check context emitted to the PR is
    # exactly `release-source` (matching the ADR-0013 ruleset's required
    # context). Multi-job workflows emit `<workflow> / <job-key>`, which would
    # never satisfy the required context.
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Skip — non-base edit (title/body) on an open PR
        # PR `edited` activity covers title/body/labels/base changes; we only
        # care about base retargets. Skip cheaply for the noisy cases so we
        # don't burn the fork check on every title tweak.
        if: >
          github.event.action != 'edited' ||
          github.event.changes.base == null ||
          github.event.changes.base.from.ref == github.event.changes.base.ref
        run: |
          echo "OK: PR edit was not a base-branch retarget; source-check N/A."

      - name: Verify head branch when base=main
        if: github.event.pull_request.base.ref == 'main'
        env:
          HEAD: ${{ github.event.pull_request.head.ref }}
          BASE: ${{ github.event.pull_request.base.ref }}
          HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          shopt -s extglob
          # Refuse to accept a same-named branch coming from a FORK. Without
          # this check, anyone with a fork named `release-0-2-0` could open a
          # PR that passes the name check while carrying untrusted code. The
          # repo must be the same as the target (this repo).
          if [[ "$HEAD_REPO" != "$REPO" ]]; then
            SAFE_HEAD="${HEAD//[^[:print:]]/?}"
            SAFE_BASE="${BASE//[^[:print:]]/?}"
            SAFE_REPO="${HEAD_REPO//[^[:print:]]/?}"
            echo "::error::PR head '$SAFE_HEAD' lives in fork '$SAFE_REPO', not this repo."
            echo "Public repository policy §11 + ADR-0013 only accept release-source heads"
            echo "that originate from $REPO. Fork PRs must be sent against a feature"
            echo "branch on this repo (or via the standard fork-then-PR workflow)."
            exit 1
          fi
          CANONICAL='^release-[0-9]+-[0-9]+-[0-9]+(-[A-Za-z0-9._-]+)?$'
          # release.yml bumps the parent pointer through a tmp/release-sync/<tag>
          # branch (see `Open and merge parent pointer PR` in release.yml). The
          # branch is auto-generated, lives in the same repo, and never sees
          # outside contributions; treat it as a release-source head so the
          # post-tag submodule bump PR can land without bypassing this check.
          # Mirror release.yml's tag construction (semver-ish: vMAJOR.MINOR.PATCH
          # with optional pre-release suffix) — refusing to match malformed tags
          # keeps the check tight against accidental name drift.
          POINTER='^tmp/release-sync/v[0-9]+\.[0-9]+\.[0-9]+(-[A-Za-z0-9._-]+)?$'
          if [[ "$HEAD" =~ $CANONICAL ]] || [[ "$HEAD" =~ $POINTER ]]; then
            echo "OK: head '${HEAD//[^[:print:]]/?}' matches a release-source pattern (base=$BASE)"
            exit 0
          fi
          # Sanitise HEAD before echoing — GitHub branch names cannot contain
          # shell metacharacters at create time, but the policy workflow must
          # not become a vector for them. Strip to printable ASCII before any
          # interpolation.
          SAFE_HEAD="${HEAD//[^[:print:]]/?}"
          SAFE_BASE="${BASE//[^[:print:]]/?}"
          echo "::error::PR targets '$SAFE_BASE' but head '$SAFE_HEAD' does not match the release-source pattern."
          echo "Public repository policy §11 + ADR-0013 require direct edits to '$SAFE_BASE'"
          echo "to flow only through a 'release-<major>-<minor>-<patch>' integration branch"
          echo "or the automated tmp/release-sync/v<tag> parent-pointer branch."
          echo "Close this PR and open a new one whose base is the active release branch"
          echo "(or whose head is renamed to release-<major>-<minor>-<patch>)."
          exit 1

      - name: Skip — not a main-targeting PR
        if: github.event.pull_request.base.ref != 'main'
        run: |
          echo "OK: base is not 'main'; source-check N/A. Stacked PRs are validated"
          echo "by the integration gate and the eventual release-source re-check"
          echo "on the release → main PR."
